export type BreadcrumbTypes = {
  label: string;
  url: string;
};
export type VMStatusTypes = 'Running' | 'Stopped' | 'Crashed';
export type StatusColorTypes = 'default' | 'secondary' | 'destructive' | 'outline';
